# Brawl Insights

Brawl Insightsは、Web版、iOS版、Android版をリリース済みです。アプリ版はCapacitorを利用。
本番環境では、Kagoya Cloud VPS (Ubuntu 24.04), Nginx, Uvicorn, Cloudflareを使って運営中。開発環境では、Uvicornを使って起動します。アプリ本体の他、バックグラウンドタスクを動かすためのワーカーとして、worker.pyとimage_worker.pyの2つがある。その他、PostgreSQLとRedisが起動してある必要があります。
本番環境は、以下のような親サーバー・子サーバーの構成で負荷分散を行い稼働しています。1. 親サーバー (メモリ16GB / CPU 8コア / NVMe SSD 1000GB): Nginxをリバースプロキシとして配置, アプリへのアクセスの 10% を処理 2. 子サーバー (メモリ8GB / CPU 6コア / NVMe SSD 800GB): アプリケーションのプロセス専任, アプリへのアクセスの 90% を処理
Web版向けにAdSense広告、アプリ版向けにAdMob広告、共通で独自広告の3種類の広告を実装済み。AdSenseとAdMobについてはポリシー違反がもし起きると非常に深刻なため、関連部分をいじる場合は十分に気をつけること。
UIについては、角丸を適切にかけ、余計な余白はカットし、美しいかつ使いやすく実装することを心がけること。また、特に指示がなくても、日本語版・ライトモードをベースに、英語版・ダークモードのUIについても実装すること。
実装したコードに、競合アプリへの模倣対策や、セキュリティ上で公開すべきでないと思われる箇所がある場合、 # PUBLIC_EXCLUDE_START と # PUBLIC_EXCLUDE_END の行で囲むこと。そうすれば、公開版リポジトリで囲んだ部分が非表示になります。
ログ出力に関しては、既存箇所を参考に適切に実装すること。
新機能を実装したとしても、特に私から指示がない場合は、todo.txt、およびバージョン履歴は勝手に編集しないこと。
DB構造を変更する場合、models.pyのみを変更すること。他にDB構造を書いているファイルは、古いものであり編集する必要はない。その後マイグレーションファイルを生成すること。最後にalembic upgrade headを実行する際は事前に私に確認すること。
CSSやJSなどの静的ファイルを変更した場合、main.pyにあるバージョン番号を1つだけ(例:1.0.10->1.0.11)上げてください(キャッシュバスティング)。
asyncpgを使う際に、json.dumpsは自動で実行されるため、さらに明示的に書いてしまうと二重でエンコードされてしまうことがあるので注意してください。
フロント上の変更がある場合は、必要に応じて開発環境でブラウザを使ったテスト等を行ってください。開発環境であれば、管理者アカウント、その他一般アカウントでのログインや操作は適宜行って構わない。なお、開発環境においては全ユーザーのパスワードは原則として12345678で固定します。